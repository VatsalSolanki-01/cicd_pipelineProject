pipeline {
    agent any

    stages {

        stage('Clone Repository') {
            steps {
                git 'https://github.com/VatsalSolanki-01/cicd_pipelineProject'
            }
        }

        stage('Build Docker Image') {
            steps {
                sh 'docker build -t go-cicd-app .'
            }
        }

    }
}